<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Video Creator | صانع الفيديو</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #0f172a; color: white; }
        .gradient-text { background: linear-gradient(90deg, #3b82f6, #8b5cf6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
    </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen p-4">

    <h1 class="text-5xl font-bold mb-6 gradient-text">Faceless AI Video</h1>
    <p class="text-gray-400 mb-8 text-xl">اصنع فيديوهات احترافية بضغطة زر واحدة</p>

    <div class="bg-slate-800 p-8 rounded-2xl shadow-2xl w-full max-w-2xl border border-slate-700">
        <label class="block mb-2 text-sm font-medium text-gray-300">أدخل موضوع الفيديو أو النص:</label>
        <textarea id="prompt" rows="4" class="w-full p-4 bg-slate-900 rounded-lg border border-slate-600 focus:ring-2 focus:ring-purple-500 outline-none" placeholder="مثلاً: قصة قصيرة عن الفضاء..."></textarea>
        
        <button onclick="generateVideo()" id="btn" class="w-full mt-6 bg-purple-600 hover:bg-purple-700 text-white font-bold py-4 rounded-lg transition-all transform hover:scale-105">
            إنشاء الفيديو الآن ✨
        </button>

        <div id="status" class="mt-4 text-center hidden">
            <p class="text-blue-400 animate-pulse">جاري معالجة الفيديو بالذكاء الاصطناعي... (هذه واجهة تجريبية)</p>
        </div>
    </div>

    <script>
        function generateVideo() {
            const btn = document.getElementById('btn');
            const status = document.getElementById('status');
            const prompt = document.getElementById('prompt').value;

            if(!prompt) { alert("من فضلك اكتب نصاً أولاً"); return; }

            btn.disabled = true;
            btn.innerText = "جاري العمل...";
            status.classList.remove('hidden');

            // ملاحظة: هنا يتم ربط الـ API الخاص بصناعة الفيديو لاحقاً
            setTimeout(() => {
                alert("تم استلام طلبك! لربط هذا الزر بمحرك فيديو حقيقي، تحتاج لإضافة API Key لخدمة مثل Replicate أو ElevenLabs.");
                btn.disabled = false;
                btn.innerText = "إنشاء الفيديو الآن ✨";
                status.classList.add('hidden');
            }, 3000);
        }
    </script>
</body>
</html>
